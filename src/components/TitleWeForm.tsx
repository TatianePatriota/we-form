import { StyledTitleWeForm } from "./StyledTitleWeForm";

export function TitleWeForm() {
  return (
    <StyledTitleWeForm>
      <h1>
        <span>We</span>Form
      </h1>
    </StyledTitleWeForm>
  );
}
